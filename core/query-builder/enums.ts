export enum SqlFunction {
  SUM = "SUM",
  AVG = "AVG",
  COUNT = "COUNT",
  MIN = "MIN",
  MAX = "MAX",
}

export enum ComparisonOperator {
  EQ = "=",
  NEQ = "!=",
  GT = ">",
  GTE = ">=",
  LT = "<",
  LTE = "<=",
}
