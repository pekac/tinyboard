export enum SqlFunction {
  AVG = "AVG",
  COUNT = "COUNT",
  MAX = "MAX",
  MIN = "MIN",
  SUM = "SUM",
}

export enum ComparisonOperator {
  EQ = "=",
  GT = ">",
  GTE = ">=",
  LT = "<",
  LTE = "<=",
  NEQ = "!=",
}
